<div class="button-group">
    <%= render CobraButtonComponent.new(
        text: @cancel_text,
        href: @cancel_url,
        close_modal: @close_modal)
    %>
    <%= render CobraButtonComponent.new(
        text: @submit_text,
        submit: true,
        close_modal: @close_modal)
    %>
</div>
