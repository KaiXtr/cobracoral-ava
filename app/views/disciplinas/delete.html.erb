<%= render CobraModalComponent.new() do %>
    <h2>Deseja realmente apagar a disciplina <%= helpers.nome_disciplina(@disciplina) %>?</h2>
    <p>
        Não será possível desfazer as alterações.
    </p>
    <div class="button-group">
        <%= render CobraButton::CobraButtonComponent.new(
            text: 'Cancelar',
            href: '/disciplinas/' + @disciplina.id.to_s + '/editar')
        %>
        <%= render CobraButton::CobraButtonComponent.new(
            text: 'Deletar',
            href: '/disciplinas/' + @disciplina.id.to_s + '/destroy',
            enable_turbo: false)
        %>
    </div>
<% end %>
