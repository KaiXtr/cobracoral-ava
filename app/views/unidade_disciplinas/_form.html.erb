<%= form_with(model: unidade_disciplina) do |form| %>
  <% if unidade_disciplina.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(unidade_disciplina.errors.count, "error") %> prohibited this unidade_disciplina from being saved:</h2>

      <ul>
        <% unidade_disciplina.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :disciplina_id, style: "display: block" %>
    <%= form.text_field :disciplina_id %>
  </div>

  <div>
    <%= form.label :nome_unidade, style: "display: block" %>
    <%= form.text_field :nome_unidade %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
