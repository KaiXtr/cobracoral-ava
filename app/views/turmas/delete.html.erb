<%= render CobraModalComponent.new() do %>
    <h2>Deseja realmente apagar a turma <%= @turma.nome_turma %>?</h2>
    <p>
        Não será possível desfazer as alterações.
    </p>
    <div class="button-group">
        <%= render CobraButtonComponent.new(
            text: 'Cancelar',
            href: '/turmas/' + @turma.id.to_s + '/editar')
        %>
        <%= render CobraButtonComponent.new(
            text: 'Deletar',
            href: '/turmas/' + @turma.id.to_s + '/destroy',
            enable_turbo: false)
        %>
    </div>
<% end %>
