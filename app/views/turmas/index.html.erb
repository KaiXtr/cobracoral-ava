<nav>
    <%= link_to "Voltar para todos os cursos", cursos_path %>
	<h1>Todas as turmas</h1>
	<% if policy(@turma).new? %>
        <%= render OpenModalButtonComponent.new(
			href: new_turma_path,
			tooltip: 'Adicionar turma')
		%>
	<% end %>
	<div>
		<%= render @turmas %>
	</div>
</nav>
