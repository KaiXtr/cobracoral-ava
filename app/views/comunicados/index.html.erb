<nav class="comunicados-nav">
  <div class="deslizar-transition">
    <h2>Comunicados</h2>
    <% if @comunicados %>
      <% if policy(@comunicado).new? %>
        <%= render OpenModalButton::OpenModalButtonComponent.new(
          href: new_comunicado_path,
          button_class: 'open-modal-comunicado',
          tooltip: 'Criar comunicado')
        %>
      <% end %>
      <%= turbo_stream_from "comunicados" %>
      <div>
        <%= render @comunicados %>
      </div>
    <% else %>
      <p>
        Você está em dia.
      </p>
    <% end %>
  </div>
</nav>
