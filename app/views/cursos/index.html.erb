<nav>
    <div>
        <h1>Meus Cursos</h1>
        <% if policy(@curso).create? %>
            <%= link_to "Adicionar curso", new_curso_path, remote: true %>
        <% end %>
        <%= render @cursos %>
    </div>
</nav>