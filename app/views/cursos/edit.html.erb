<nav>
    <% if policy(@curso).destroy? %>
        <a
            href="/cursos/<%= @curso.id %>/deletar"
            class="delete-button"
            data-turbo-frame="cobra-modal"
            data-remote="true">
            <i class="fa-solid fa-trash fa-lg icon-button"></i>
        </a>
    <% end %>
    <%= render "form", curso: @curso %>
    <div class="disabled-content">
        <h2>Minhas turmas</h2>
        <div>
            <%= render @turmas %>
        </div>
    </div>
    <div class="disabled-content">
        <h2>Minhas disciplinas</h2>
        <h3>Semestre</h3>
        <div class="disciplinas-container">
            <%= render @disciplinas %>
        </div>
        <h3>Semestre</h3>
        <div class="disciplinas-container">
            <%= render @disciplinas %>
        </div>
    </div>
</nav>